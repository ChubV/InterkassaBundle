imports:
    - { resource: interkassa.yml }

framework:
    secret:          123
    router:
        resource: "%kernel.root_dir%/routing.yml"
        strict_requirements: %kernel.debug%
    templating:      { engines: ['twig'] } #assets_version: SomeVersionScheme
    test: ~
